


TRUE /\ {A} |- TRUE /\  ({})^*.{A};

TRUE /\ ({A, D, C})^*.{} \/ TRUE /\  {B}.({A})^* |- TRUE /\ ({})^* \/ TRUE /\ ({A,B})^*;

TRUE /\ {} |- TRUE /\ ({})^*;

{A, B, C}.{}.{C, D, E} |- {A}.{B};

({})* \/ {A, B}.({})* |- {C, D};

{B, D}.{A, C} |- {D}.({})*.{C};

{A, B} \/ {} \/ {D}.{E, F} |- {C, D}.({})* \/ ({})*.{E};

{}.({A, B})* |- {A, B};